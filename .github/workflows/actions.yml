name: react-testing

on: [ push ]

jobs:
  execute_tests:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: use node
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run: yarn install
      - run: yarn run test